# --- Core Utilities ---
python-dotenv>=1.0.0
pillow>=10.0.0

# --- Azure ---
# These are safe and rarely conflict with ML libraries
azure-core>=1.26.0
azure-search-documents>=11.0.0b5

# --- Computer Vision & AI ---
# Warning: These depend on PyTorch. 
# We install them *after* your manual GPU install.
transformers>=4.40.0
roboflow>=1.2.11

# Ultralytics often creates the most conflicts.
# We trust it will accept the Torch version we already installed.
ultralytics>=8.3.235

#pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126